services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bun-react-starter
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: file:./dev.db
      JWT_SECRET: Pvv5LksrsAKUPbikhJtmOSjnIreuegKEasSnnpzOqkM=
    volumes:
      - db_data:/app/packages/database/prisma
    restart: unless-stopped

volumes:
  db_data:
